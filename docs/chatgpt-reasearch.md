Правила, команди та навички для агентної IDE: Теоретична частина

Вступ: Від «просто промптинг» до системи правил і команд
Ad-hoc промптинг vs. системний підхід. Коли ми кожен раз пишемо новий промпт, агент не має пам’яті про попередні домовленості – доводиться повторювати контекст, і результат може бути нестабільним. Натомість система правил і команд забезпечує сталі інструкції: агент щоразу стартує із заданими рамками, стилем та обмеженнями. Такий “статичний контекст” економить час і токени, бо не потрібно дублювати однакові вимоги у кожному запиті[1][2]. До того ж, розбиття інструкцій на рівні (системні правила + конкретні команди) покращує ясність. Один великий prompt може бути неоднозначним і важким для підтримки, тоді як чітка структура підказок підвищує якість відповіді: “Clear structure and context matter more than clever wording—most prompt failures come from ambiguity, not model limitations.”[3] Іншими словами, краще визначити агенту чіткі ролі та правила, ніж сподіватися на магію від одноразового запиту.
Керування агентом vs. просто інструмент. При «просто промптингу» AI діє як чорний ящик, і розробник реагує на його вихід постфактум. У системному підході ми проактивно керуємо агентом: задаємо політику (правила) і сценарії дій (команди), перетворюючи його на повноцінного помічника, що працює за узгодженими принципами. Це робить взаємодію передбачуваною та повторюваною – критично важливо для командної роботи і великих проектів.
Типи інструкцій у агентних IDE
Багаторівневі правила. Сучасні Agentic IDE дають можливість визначати інструкції на різних рівнях: - Системні (глобальні) правила: загальні принципи і поведінкові установки, що застосовуються для всієї роботи AI. В Cursor, наприклад, глобальні правила задаються в налаштуваннях IDE і визначають “core principles for all AI interactions”[4]. Вони мова-незалежні і формують базовий “job description” моделі[5].
- Правила проєкту: специфічні для конкретного репозиторію/проєкту інструкції. У Cursor є файл .cursorrules (або папка .cursor/rules/ з RULE.md), куди вносяться домовленості саме цього проєкту (технології, архітектурні патерни, стилі коду)[6]. В GitHub Copilot аналог – файл .github/copilot-instructions.md у репозиторії, що дає Copilot контекст про проект (як збирати, тестувати, які стандарти)[7][8].
- Локальні/ієрархічні правила: інструкції, що застосовуються до окремих файлів чи шаблонів. Cursor підтримує pattern-specific rules – файли .mdc з YAML, де через globs задається, до яких файлів застосувати правило[9]. Copilot дозволяє path-specific instructions (файли NAME.instructions.md з полем applyTo для шляхів)[10][11]. Це дозволяє, наприклад, мати окремі правила стилю для frontend vs backend файлів, або особливі вимоги до тестів у папці tests/.
Правила для різних ролей. Агент може виконувати різні завдання – написання коду, генерування тестів, аналіз чи рев’ю. Для кожної ролі корисно мати свій набір правил: - Для розробника (Dev): правила код-стайлу, архітектурні принципи, оптимізації продуктивності.
- Для тестувальника (QA): правила написання тест-кейсів, перевірки граничних випадків, валідації вимог.
- Для code reviewer: критерії якості коду, безпеки, відповідності конвенціям, щоб агент не тільки генерував код, а й умів перевірити чужий.
- Для архітектора: високорівневі обмеження (наприклад, “усі модулі комунікують через API, прямі виклики заборонені”), технологічні стандарти, ліцензійні обмеження.
Наприклад, у Cursor можна згрупувати правила за категоріями: Core Rules, Integration Rules, Pattern/Role Rules – що відповідає базовим принципам, інтеграційним моментам та спеціалізованим інструкціям для окремих шаблонів чи ролей[12][13]. Така ієрархія допомагає структурувати знання, аби агент завжди мав “настанови старшого колеги” під рукою, навіть якщо контекст розмови очищено.
Огляд інструментів: Cursor vs Claude Code vs GitHub Copilot
Cursor (IDE). Cursor – це окрема IDE з вбудованим AI-помічником (працює на моделях Anthropic Claude чи GPT). Сильна сторона Cursor – глибока інтеграція з кодом: він будує індекс по всьому проекту, дозволяє семантичний пошук по репозиторію і миттєво знаходить потрібний контекст при запитах[14][15]. Cursor надає Multi-agent режим: можна запускати паралельно декілька агентів (на різних моделях) для різних стратегій вирішення задач[16][17]. Інструмент орієнтований на швидкі і точкові зміни: “Cursor is great if you already know what you’re doing and want to do very fast targeted development... Cursor wins here for speed and efficiency.”[18][19].
Для кастомізації поведінки Cursor пропонує: - Rules: статичні правила, що завжди додаються в контекст (папка .cursor/rules/). Це зручно для підтримки стилю кодування та стандартів проекту[20][21].
- Commands: користувацькі команди (швидкі сценарії) доступні через інтерфейс IDE. Їх можна настроїти для команди, але створювати можуть адміни[22].
- Skills: навички агента – динамічні підключаємі модулі. Agent Skills – це відкритий стандарт (від Anthropic) для додавання знань або інструментів агенту. Наприклад, скіл “skill-creator” допомагає генерувати нові навички[23]. Навички можуть містити кастомні команди, хуки або доменний інструктаж, які агент підгружатиме за потреби[24][25].
Claude Code (CLI/VS Code). Claude Code – це агент від Anthropic, який часто використовується через розширення до VS Code або CLI. Він побудований на моделях Claude з дуже великою пам’яттю (контекст до 100k+ токенів). Особливість Claude Code – підхід з одним структурованим планом: агент аналізує завдання, пропонує план рішень (Plan mode) і виконує його з підтвердженням користувача[26][27]. Claude Code підтримує sub-agents – допоміжні підзадачі в межах єдиного процесу, які координуються центральним агентом (на відміну від паралельних агентів у Cursor)[28]. Також Claude Code має: - Checkpoints: знімки стану після кожного кроку, щоби можна було відкотити невдалий рефактор легко[29].
- Hooks: скрипти-хуки для автоматизації – “Automation triggers... to enforce workflows, validate changes, and run tests without prompting.”[30]. Наприклад, можна налаштувати, щоб після кожного завершеного завдання автоматично запускались тести і при падінні тестів агент сам вносив правки.
- Skills: Claude Code теж підтримує підключення Agent Skills (стандарт Anthropic), розширюючи свої можливості скриптами або інструментами так само, як Cursor.
Claude Code чудово працює на великі зміни: міграції, рефакторинг по всьому монорепозиторію, аналіз багатьох файлів одночасно. “Claude Code shines when you need deep, uninterrupted reasoning across a huge system… for architecture-level work in big codebases.”[31][32]. Проте він може бути занадто “розумним” для дрібних задач – розробники відзначають, що Claude іноді “перевиконує” задачу, відволікаючись на несуттєві зміни, і важко сфокусувати на чомусь малому[33]. В таких випадках саме ізольований агент Cursor працює точніше.
GitHub Copilot. Copilot інтегрується у популярні IDE (VS Code, JetBrains) і надає переважно inline-підказки та чат. Його контекстне вікно менше (для GPT-4 ~8k токенів), але Copilot робить попередню індексацію репозиторію: будує векторне представлення коду, щоб при запитах витягати дотичні фрагменти. Для налаштування поведінки Copilot пропонує Custom Instructions: - Особисті інструкції (для конкретного користувача облікового запису), - інструкції репозиторію (файл .github/copilot-instructions.md у проекті), - організаційні інструкції (для всіх repo компанії)[34][7]. Через них можна задати стиль, технологічний стек, командні практики, які Copilot завжди враховуватиме. Наприклад, у репозиторій можна додати секцію “Як будувати і тестувати проект” – Copilot буде це читати і генерувати код з урахуванням цих вказівок. Copilot Chat також підтримує References – він показує, які файли (в т.ч. ваші інструкції) були використані при формуванні відповіді[35]. Проте гнучкість Copilot в плані агентності менша: ви не можете створити свої “команди” чи підпроцеси, він не запускає ваші тести автоматично (натомість GitHub пропонує окремі Copilot Agents для Pull Requestів, CLI тощо, але це поки менш зріле). Таким чином, Copilot добре підходить для авто-доповнення коду за стилем проекту, але для складних сценаріїв (ланцюжків дій, інтерактивного планування) він поступається спеціалізованим агентним IDE.
(На практиці багато команд комбінують інструменти: наприклад, пишуть код у Cursor, а рев’юють з Copilot, або тримають обидва – якщо Claude плутається, перейти в Cursor, і навпаки[36][37].)
Як перетворювати вимоги та обмеження на правила
Витягання правил із вимог. У реальних проектах вимоги існують у вигляді PRD, user story, або негласно “в голові у сеньйора”. Завдання – зробити їх явними для AI. Для цього треба проаналізувати вимоги і сформулювати сталі правила, яких має дотримуватися агент при написанні коду:
•	Бізнес-обмеження і безпека. Якщо вимога каже “паролі не зберігаємо у відкритому вигляді” – формалізуємо правило: “Всі паролі повинні хешуватися алгоритмом X з сіллю; не зберігати чутливі дані в коді.” Якщо система має працювати під великим навантаженням – правило: “Дотримуватися O(n) або кращої складності для операцій пошуку; уникати блокуючих викликів у головному потоці.” Правила безпеки включають: не використовувати заборонені порти/функції, перевіряти вхідні дані, слідувати вимогам стандартів (наприклад, OWASP для веб). Це запобігає тому, щоб агент згенерував небезпечний або неетичний код.
•	Ліцензійні обмеження. Агент не завжди “знає” про ліцензії, тому якщо проект, наприклад, не може використовувати GPL-бібліотеки, варто додати правило: “Використовувати тільки пакети з ліцензією MIT/Apache; код, що генерується, не повинен містити скопійований фрагмент GPL.” Так само можна задати список схвалених залежностей (allow-list) або заборонених (deny-list). Це збереже час на рев’ю, адже агент одразу шукатиме альтернативи, сумісні з ліцензією.
•	Стандарти якості та архітектурні принципи. З вимог та дизайну витягуємо загальні принципи: наприклад, “усі API виклики мають проходити через шар сервісів, а не напряму з контролера”, або “інтерфейси UI не звертаються до бази напряму – тільки через API бекенда”. Такі принципи оформлюємо як правило, і тоді агент генеруватиме код, що їх дотримується. Якщо в проекті домовлено про певний code style/лінтер – варто виписати основні моменти (наприклад, “використовуємо тільки ES6+, стрілочні функції, ніяких var” для JS проекту). Не слід копіювати цілі стандарти (наприклад, Google Style Guide) дослівно в правила – достатньо дати кілька ключових тез і посилання на приклад у коді[38][39]. Правило може навіть відсилати до конкретного файлу як еталону: “Дотримуватися структури, як в components/Button.tsx для UI-компонентів”[40] – агент сам подивиться цей файл як приклад.
Приклад: припустимо, є user story: “Як користувач, я хочу отримувати email-підтвердження при реєстрації.” Звідси можна виділити потенційні правила: (1) Валідація email: “Перевіряти формат email regex’ом і на унікальність перед створенням аккаунта”; (2) Надсилання листа: “Використовувати SMTP сервіс X, шаблон листа брати з файлу welcome_email.html”; (3) Безпека: “Токен підтвердження має бути одноразовим, дійсний 24 години”. Внісши ці пункти у проектні правила, ми гарантуємо, що агент їх не пропустить коли генеруватиме код фічі.
Також варто фіксувати в правилах знання про нові версії технологій: якщо в проекті щойно оновилися бібліотеки, можна додати: “Використовувати хук useNewApi замість застарілого useOldApi (з версії 3.0)” або “Модулі переводимо на Kotlin DSL замість Groovy (Gradle 8+)”. Це дозволяє агенту бути в курсі останніх змін і не пропонувати старі підходи.
Приклади правил у коді
Правила оформлюються текстово (Markdown), часто списком bullet points. В Cursor проектні правила зберігаються у .cursor/rules/RULE.md, а в Copilot – у .github/copilot-instructions.md. Кілька прикладів із реальних правил:
# Commands
- `npm run build`: Build the project  
- `npm run typecheck`: Run the typechecker  
- `npm run test`: Run tests (prefer single test files for speed)

# Code style
- Use ES modules (import/export), not CommonJS (require)
- Destructure imports when possible: `import { foo } from 'bar'`
- See `components/Button.tsx` for canonical component structure

# Workflow
- Always typecheck after making a series of code changes
- API routes go in `app/api/` following existing patterns
Цей фрагмент демонструє правила для JavaScript-проєкту: секція “Commands” описує команди для збірки/тестів, “Code style” – стиль коду, “Workflow” – процес розробки. Агент завжди бачить ці інструкції на старті кожної сесії, тому знає, які команди запускати і яким стандартам слідувати[41][42]. Важливо тримати правила лаконічними і актуальними. Рекомендовано зберігати файли правил у репозиторії (під контролем версій) і оновлювати їх щоразу, як знайдена нова помилка або вводиться нова конвенція – тоді всі члени команди та їхні AI-асистенти будуть на одній хвилі[43].
Типові помилки та антипатерни при роботі з правилами
Навіть правильно сформульовані правила можуть не дати ефекту, якщо припуститись організаційних помилок:
•	Надто довгі або деталізовані правила. Велика спокуса – виписати “все і одразу” у системні інструкції. Але занадто довгий prompt шкодить: він займає багато місця у контексті, що зменшує “пам’ять” для корисної історії, та може “quietly drain accuracy, speed, and budget” моделі[44][45]. Як зазначають експерти, “be careful of extremely lengthy system messages that could overload the model and contain contradicting information.”[46] Якщо правил надто багато, модель може їх просто ігнорувати[47][48]. Тому краще залишати тільки суттєве: замість вставляти цілий POSIX стандарт – дати пару прикладів і сказати “дотримуйся стандарту, деталі за посиланням”. Так агент не “потоне” в інструкціях.
•	Суперечливі правила. Якщо різні правила конфліктують (одне каже “використовуй таби”, інше – “використовуй пробіли” 😅), то агент не зможе виконати обидва. В системах типу Cursor діє принцип: останнє правило має перевагу (як останній налаштований термостат)[49]. Але краще взагалі уникати конфліктів: визначити однозначну політику і видалити дубльовані або протилежні пункти. Поширений антипатерн – копіювати в різні rule-файли схожі інструкції, які потім розходяться. Використовуйте централізовані правила та єдиний стиль формулювань, щоб не було неясності.
•	Правила, яких неможливо дотриматися технічно. Іноді пишуть вимоги на кшталт “код повинен працювати за O(1) для всіх операцій” або “додаток не має містити жодної вразливості” – тобто утопічні формулювання. Модель не чарівник: якщо задача в принципі нерозв’язна, то й правило не допоможе. Агент може почати “галюцинувати” виконання неможливого або проігнорує недосяжну вимогу. Правила мають бути реалістичними і перевіряними. Краще розбити загальну вимогу на конкретні підправила: замість “код без вразливостей” прописати “не використовувати eval, перевіряти довжину масивів перед доступом по індексу, не логувати секретні ключі” тощо. Так агент матиме чіткі кроки, як досягти безпечності.
•	“Магічні” промпти без критеріїв успіху. Якщо в команді для агента написати розпливчато “Зроби код кращим” або “Оптимізуй все”, результат непередбачуваний. Модель може піти у неправильному напрямку, бо не знає, що саме означає “кращий” – менше рядків? швидше виконання? інший дизайн? Без чітких критеріїв це запрошення до самодіяльності. Антипатерн – давати AI завдання без визначення “Definition of Done”. Краще: “Поліпши читабельність коду: виділи магічні числа у константи, додай коментарі до складних блоків.” Або: “Оптимізуй по пам’яті: зменши пік використання RAM нижче 500MB.” Тоді агент має конкретну мету. Неявні побажання часто приводять до розчарування, адже AI не вгадає, що саме ви мали на увазі.
Насамкінець, уникайте спроб “обдурити” модель занадто хитрим prompt’ом. Як зазначає керівник PromptHub, іноді варто просто розділити задачу на серію простіших кроків, ніж писати один гігантський запит, який намагається врахувати все[50][51]. Дотримання цих принципів допоможе зробити правила справді ефективними.
Підходи до підвищення якості та безпеки з допомогою правил
Зменшення галюцинацій та “самодіяльності”. Одне з ключових питань – як зробити так, щоб агент видавав стабільно коректний код, не додавав лишнього від себе. Правила і контекстні документи – наше рішення. Наприклад, техніка Memory Bank дозволяє агенту “пам’ятати” прийняті рішення і не пропонувати їх знову або не суперечити їм. Memory Bank – це набір файлів (наприклад, architecture.md, decisions.md, changelog.md), які зберігаються в проекті і завжди підвантажуються для контексту[52][53]. Таким чином, AI усвідомлює історію проекту. Результат – модель менше фантазує: “This keeps the AI grounded and reduces hallucinations or rework.”[54] – тобто агент не буде пропонувати рішення, які вже відкидалися раніше, і не забуде про правки, зроблені на попередніх ітераціях.
Так само правила можуть зменшити “творчість не до теми”. Якщо задати обмеження “не змінювати існуючий публічний API без дозволу” або “не генерувати код поза межами поточного модуля”, агент не полізе рефакторити весь проект, коли його просять додати одну кнопку (це іноді трапляється з потужними моделями без стримування)[33]. Чіткі рамки = менше несподіванок.
Стандарти якості коду та ліцензії. Як згадано, варто явно вказувати вимоги до стилю та якості. Агент може автоматично проставляти ліцензійні хедери в нових файлах, якщо правило каже: “кожен файл має починатися з коментаря про ліцензію”. Можна прописати: “при генерації коду додавати док-коментарі до функцій публічного API”, і AI це робитиме, економлячи час рев’юверів. Якщо у вас прийнято писати юніт-тести на кожен метод – створіть навичку/команду, що після генерації коду автоматично викликає агенту: “створи файл тестів для цього коду за правилами”. Наприклад, команда “Generate Tests” може включати інструкцію: “Згенеруй тест-кейс(и) для щойно створеного методу, покриваючи основні сценарії і крайні випадки, перевір відповідність бізнес-вимогам”. Отримавши такий наказ, IDE-агент зробить потрібні тести одразу.
Вбудовування перевірок у команди. Дуже ефективний патерн – вбудувати у роботу агента кроки контролю якості. В Cursor і Claude можна налаштувати, щоб після виконання команди агент автоматично запустив лінтер, тести, або навіть інший агент-рев’ювер. Як згадувалось, Claude Code має hooks: можна поставити hook на подію “stop” (завершення завдання) – скрипт, який запускає npm test і в залежності від результату або дає follow-up повідомлення агенту виправити помилки, або продовжує далі[55][56]. Це фактично цикл “запускайся, поки всі тести не пройдуть” – AI сам і виправляє свій код, поки не досягне зелених тестів. У правила можна включити інструкції для code review: наприклад, шаблон “Review Checklist” з пунктами безпеки, продуктивності, стилю – і мати команду “Review Code” яка заставляє агента проаналізувати diff чи файл за цим чеклістом. Тоді агент виступить у ролі рев’ювера, видаючи зауваження, які можна виправити тими ж правилами.
Приклад інтеграції перевірки: у правилі або навичці описано: “Якщо згенеровано SQL запит – автоматично виконати EXPLAIN для перевірки індексів” або “Після генерації Dockerfile – запустити docker lint і проаналізувати результати”. Агент, знаючи це, зробить додаткові кроки, не чекаючи поки людина вручну знайде проблему. Це створює своєрідні “рейки безпеки” (guardrails), що не дозволяють йому виїхати за межі допустимого. В підсумку, команда отримує більш надійний код від AI, де помилки відловлюються на етапі генерації, а не в продакшені.
Патерни роботи з правилами (Memory Bank та інші)
Memory Bank як довгострокова пам’ять. Як вже згадували, Memory Bank – це структура файлів, що зберігає важливу інформацію про проект для агентів. Типові файли: projectBrief.md (цілі проекту), systemPatterns.md (архітектурні патерни), techContext.md (стек і версії), progress.md (лог виконаних задач) тощо[57]. В Copilot їх можна розмістити у .github/copilot-instructions.md як секції, а в Cursor – підвантажувати через правила або навички. Цей патерн дозволяє новому агенту швидко “вникнути” в контекст: замість читати всі вихідні коди, він отримує стиснутий знаннями конспект. Крім зменшення галюцинацій, Memory Bank дає покращення співпраці: “Anyone (AI or human) can quickly get up to speed... Memory Bank becomes your single source of truth.”[58]. Важливо підтримувати ці файли актуальними – оновлювати при зміні вимог, додавати рішення архітектурних проблем, фіксувати прийняті стилістичні вибори. Так ви буквально навчаєте агента на ходу, поступово збагачуючи його “знання” про проект.
Ітеративне вдосконалення правил. Співпраця з AI – це цикл: написав правило → отримав код → проаналізував → допрацював правило. Корисно почати з мінімального набору правил, а далі розширювати їх на основі помилок агента. Якщо AI щось зробив неправильно і це не було обумовлено – додаємо нове правило, щоб надалі такого не траплялося[59]. Наприклад, помітили, що агент постійно забуває закривати ресурс – додайте: “завжди закривати Connection у блоці finally”. Наступного разу він це врахує. Так крок за кроком ви еволюційно вибудовуєте бібліотеку правил, орієнтовану саме на ваш проект і команду.
Прозорість та командне використання. Документація правил має бути доступна всім новим членам команди. Рекомендується завести файл README з описом, як користуватися AI в цьому проекті: де лежать правила, які є команди, як запускати агента. Новачок, ознайомившись, одразу працюватиме продуктивніше і не буде боятися “зламати щось” агентом. Стандартизуйте підхід: наприклад, мінімальний набір правил для кожного нового проекту (безпека, стиль, CICD вимоги), шаблони команд для типових задач (створення фічі, виправлення багу, налаштування оточення). Це створює культуру взаємодії з AI: замість хаотичного промптингу, всі дотримуються єдиних практик. В результаті – якість AI-коду стає передбачуваною, а витрати токенів та часу – підконтрольними і прозорими.
Q&A
На завершення теоретичної частини підсумуємо: ефективна робота з AI-агентами в IDE потребує планування і структури. Ми розглянули, як правила (static instructions) задають рамки, команди запускають сценарії, а навички розширюють можливості агента. Обговорили типові помилки та як їх уникнути. Якщо у вас залишились питання – саме час їх поставити! Наприклад: “Чи завжди варто писати правила українською, чи агент краще розуміє англійську?” або “Як побудувати Memory Bank для legacy-проекту?”. Давайте обговоримо 🙂. (Далі – практична сесія, де всі ці принципи застосуємо hands-on.)
Джерела: Основні ідеї та приклади взято з офіційних документaцій Cursor, GitHub Copilot та блогів про досвід використання агентних IDE[21][6][17], а також зі спільноти розробників (обговорення на форумах, статті на Medium)[19][60]. Ці джерела підтверджують важливість правильних правил: “Don’t over-optimize before you understand your patterns... add rules only when agent makes the same mistake repeatedly.”[59]. Застосовуючи ці найкращі практики, ви зможете перестати “вигадувати промпт щоразу з нуля” і почати системно керувати AI-розробкою.
________________________________________
[1] [2] [52] [53] [54] [57] [58] Memory Bank System | Agentic Coding Handbook
https://tweag.github.io/agentic-coding-handbook/WORKFLOW_MEMORY_BANK/
[3] The Ultimate Guide to Prompt Engineering in 2025 | Lakera – Protecting AI teams that disrupt the world.
https://www.lakera.ai/blog/prompt-engineering-guide
[4] [6] [9] Mastering Cursor Rules: A Developer's Guide to Smart AI Integration - DEV Community
https://dev.to/dpaluy/mastering-cursor-rules-a-developers-guide-to-smart-ai-integration-1k65
[5] [44] [45] Why Long System Prompts Hurt Context Windows (and How to Fix It) | by Lucas Valbuena | Data Science Collective | Medium
https://medium.com/data-science-collective/why-long-system-prompts-hurt-context-windows-and-how-to-fix-it-7a3696e1cdf9
[7] [8] [10] [11] [35] Adding repository custom instructions for GitHub Copilot - GitHub Docs
https://docs.github.com/en/copilot/how-tos/configure-custom-instructions/add-repository-instructions
[12] [13] [49] My Best Practices for MDC rules and troubleshooting - Guides - Cursor - Community Forum
https://forum.cursor.com/t/my-best-practices-for-mdc-rules-and-troubleshooting/50526
[14] [15] [20] [21] [24] [25] [38] [39] [40] [41] [42] [43] [55] [56] [59] Best practices for coding with agents · Cursor
https://cursor.com/blog/agent-best-practices
[16] [17] [26] [27] [28] [29] [30] [31] [32] Cursor vs. Claude Code: in-depth comparison for dev teams | DECODE
https://decode.agency/article/cursor-vs-claude-code/
[18] [19] [33] [36] [37] Difference between using cursor and claude code? : r/ChatGPTCoding
https://www.reddit.com/r/ChatGPTCoding/comments/1l8w2h4/difference_between_using_cursor_and_claude_code/
[22] Commands | Cursor Docs
https://cursor.com/docs/agent/chat/commands
[23] IS Skills Supported - Help - Cursor - Community Forum
https://forum.cursor.com/t/is-skills-supported/146837
[34] Configure custom instructions for GitHub Copilot - GitHub Docs
https://docs.github.com/en/copilot/how-tos/configure-custom-instructions
[46] [50] [51] [60] The Difference Between System Messages and User Messages in Prompt Engineering
https://www.prompthub.us/blog/the-difference-between-system-messages-and-user-messages-in-prompt-engineering
[47] [48] Rules Hierarchy in Cursor - Discussions - Cursor - Community Forum
https://forum.cursor.com/t/rules-hierarchy-in-cursor/108589
