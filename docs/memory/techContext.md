# Tech Context

## Stack

- Slidev + Vue 3
- Node 20+
- npm

## Commands

- `npm run dev` — local dev
- `npm run build` — production build
- `npm run export` — export slides

## Assets

- Static assets live in `public/` and are referenced as `/asset.ext`

